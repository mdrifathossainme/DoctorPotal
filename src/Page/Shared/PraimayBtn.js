import React from 'react';

const PraimayBtn = ({children}) => {
    return (
        <div>
      <button class="mt-8 btn bg-gradient-to-r from-secondary to-primary text-white upparcas ">{children}</button>
            
        </div>
    );
};

export default PraimayBtn;